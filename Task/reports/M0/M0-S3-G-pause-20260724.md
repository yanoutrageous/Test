# M0-S3-G 最小可恢复暂停交接（2026-07-24）

## 暂停结论

- 暂停时间：2026-07-24 13:21:17 +08:00。
- 原因：用户要求在最小可暂停点停止并标记目标暂停，等待下一次明确回复。
- 当前切片：`M0_S3G_QUARANTINE_RETAINED_RESTORE`。
- 这是未验收的 S3-G 实现检查点，不是 S3-G、S3、M0 或 M0—M5 的完成点。
- 暂停前没有活动的仓库 Python、pytest 或 Git 进程，没有业务 mutation，没有处理真实业务数据；production writer 仍断开。
- 活动数据库`data/db/question_bank.sqlite3`的 SHA-256 为`1505BF05BD8E385EADA30642110596363C561C330DA02A40A497072064AD1C94`。

## Git 与候选快照

- 分支：`agent/m0-m5-local-v1`。
- 候选开始前本地与 origin 均为`d9f047734b6715110e10479801798864a878a184`。
- S3-G WIP 检查点：`aec3d7413a06251e4865b601fd680090b466892e`。
- WIP tree：`868ebdfc0c996cead017741e0c73ae8d08b46d8d`。
- 暂停元数据写入前的候选组合指纹：`41b4b7cbef58a115e3237d05d6b3cb7eb6c286b1f9c6b8fdbf0cc2377a787beb`。
- 最后一个已验收功能检查点仍是`f9756df79d979ef10f54fc4634f15849857019da`；WIP 提交只用于迁移和恢复，不代表功能通过。

候选文件：

| 文件 | 字节 | SHA-256 |
| --- | ---: | --- |
| `app/safety/job_operation.py` | 128277 | `1b5d8a60729799be47e1bb5f3da5014757cb732251dcce69e759ccbb055382b9` |
| `app/safety/production_guard.py` | 200563 | `5a3b4afdf458fd643b99e62789f54fd2c0a473cf4f7475ee60d08dbd0320a3a9` |
| `app/safety/windows_handle_writer.py` | 206638 | `7a79ac8a485edfc970214253dd4aee392fe26a072c698805c0e59b98d31a2a70` |
| `scripts/run_safe_pytest.py` | 90158 | `041ef75559c419f8b0e7af378f1157338ff41cf2f214b33d2b5621a9e8f61bcb` |
| `tests/test_safe_pytest_launcher.py` | 55241 | `00917f41f471fccacc4966d973289e0bf3ccce3cd342c28e6255c896b986db46` |
| `tests/test_quarantine_restore_operation.py` | 14665 | `4b97f3fdd230f3325cadbbf3f6347515497f812816e8d78656282278438b0865` |

## 已完成但尚未验收的候选内容

- job-bound quarantine pair 的签发、预留与运行时回放候选。
- Windows handle writer 的 quarantine 目录移动与读取观察候选。
- quarantine 与 retained restore 的 receipt、prepare/authorize/execute 和 journal 候选。
- internal quarantine locator、manifest、冲突不覆盖及恢复路径的候选测试。
- safe launcher 的`s3g`与`s3g_core`模式候选。

以上文件已通过 Python 语法编译检查；候选差异通过`git diff --check`。功能测试、攻击矩阵和完整安全门尚未通过。

## 失败运行与安全事实

### `RUN-20260724-M0-S3G-S3E-039`

- 模式：`s3e_core`。
- pytest/effective exit code：120。
- 失败原因：启动工具超时返回后关闭 stdout，pytest 在终端 flush 时触发`OSError: [Errno 22] Invalid argument`；不是产品断言失败，但不能作为通过证据。
- 已确认安全门：134,219 个保护树对象前后 digest 相同；数据库哈希相同；watcher、句柄围栏、run tree、immutable evidence、进程树和 source witness 均通过。

### `RUN-20260724-M0-S3G-CORE-040`

- 目标模式：`s3g_core`。
- pytest 未启动，未生成`run-result.json`。
- 安全停止：`SAFETY_STOP: cannot fence protected-tree object: Windows error 32`。
- 原因：用于规避 stdout 断管的重定向日志文件被放在受保护`tmp`、但不在排除的 run root 内；启动进程持有该文件句柄，句柄围栏按设计停止运行。

两个 run ID 都已使用，恢复时不得复用。

## 已知未完成项

- 新增 S3-G 测试尚未完成一次有效运行。
- runtime 尚未实现 quarantine 的日期分区创建；当前候选测试预先创建目录。
- 候选 API、恢复拓扑、冲突路径和清理路径尚未通过功能与攻击矩阵验证。
- 生产静态审计、inventory 和对应证据尚未更新。
- S3-G 未验收；不得进入 S3-H，不得连接 production writer，不得触碰真实业务数据。

## 精确恢复顺序

1. 收到用户明确回复后，重新阅读`AGENTS.md`、`Base/Base.md`、`Task/CURRENT_STATUS.md`、`Task/RUN_STATE.json`和本报告。
2. 核验当前分支、本地/origin 提交一致、工作区干净、WIP tree、无活动 launcher/业务 mutation、数据库哈希与本机 portable-root 合同。
3. 先修复 safe launcher 的子进程输出捕获：stdout/stderr 文件必须由 launcher 放在排除的唯一 run root 内，不能在受保护树其他位置保持打开句柄。
4. 使用新的唯一 run ID 运行`s3g_core`；保留任何失败证据，不复用编号。
5. 修复 S3-G 功能问题并实现 quarantine 日期分区；依次完成 focused、launcher、combined 安全回归。
6. 回归通过后更新静态 audit/inventory，做精确暂存审计，再决定 S3-G 验收提交；在此之前不进入 S3-H。
