# M0-S3-F 安全暂停交接（2026-07-13）

## 暂停结论

- 暂停时间：2026-07-13 22:00:02 +08:00；原因：用户要求在最近可恢复断点暂停。
- 这是 S3-F 实现中断点，不是 S3-F 验收点，也不是 M0 完成点。
- 暂停时没有活动 safe launcher、Git 进程或业务 mutation；未读取或处理真实业务资料，production writer 继续断开。
- 本地与 origin head 在写入暂停记录前均为`01cfbb12012040b03e82745e828cf3ff85dc1bf1`。
- 活动数据库`data/db/question_bank.sqlite3`的 SHA-256 为`1505BF05BD8E385EADA30642110596363C561C330DA02A40A497072064AD1C94`；D 盘可用 588,403,019,776 bytes。

## 已保留的候选状态

暂停记录前工作区有 36 个 tracked modified 和 7 个 untracked 文件。核心新增文件为：

- `app/safety/external_source.py`
- `app/safety/copy_ledger.py`
- `app/safety/copy_operation.py`
- `tests/test_external_source.py`
- `tests/test_copy_ledger.py`
- `tests/test_copy_operation.py`
- `tests/test_policy_epoch_compatibility.py`

其余 tracked 修改包含 S3-F 所需的 context、policy、operation/segment ledger、writer/static audit、safe launcher、测试和 inventory 候选。它们尚未通过最终回归，禁止清理、覆盖、批量格式化、`git checkout --`、`git reset --hard`或`git clean`。

## 当前证据

- 修复前安全运行`RUN-20260713-M0-S3F-INTEGRATION-020`：JUnit 167 tests、17 failures、0 errors、0 skipped，pytest/effective exit code 均为 1。
- 同一运行的数据库、保护树、运行时 watcher、句柄围栏、合成源 witness、进程树、immutable evidence 和 run tree 安全门均通过；该 RUN 只可作为失败诊断证据，不能作为验收证据，也不得复用。
- 17 项对应的 ledger 夹具、恢复 one-shot verification、执行 envelope、existing-target seal、V4 HMAC plan、异常 traceback 脱敏、长路径 launcher 与 opaque TXN 通道修复均已进入当前工作区。
- 相关模块`py_compile`和定义导入已通过。
- V4 Copy plan/opaque TXN 最终独立只读复审：P0=0、P1=0。
- 异常 boundary 代码独立只读复审：P0=0、P1=0。后续发现 ADR 对 hostile same-process reflection 的安全承诺过宽；ADR/CURRENT_STATUS 文案已修正，但最终文档复审因用户暂停被中断，必须在恢复后先收口。

## 保留且未使用的运行编号

以下目录在暂停前均不存在，恢复核验后才可各使用一次：

1. `RUN-20260713-M0-S3F-INTEGRATION-021`（`s3f_core`）
2. `RUN-20260713-M0-S3F-LAUNCHER-022`（`launcher`）
3. `RUN-20260713-M0-S3F-COMBINED-023`（`s3f`）

任何一次若已创建，即使启动失败也不得复用；应选择新的唯一编号并记录原因。测试只允许通过`scripts/run_safe_pytest.py`启动，禁止直接调用 pytest；safe launcher 活动期间禁止 Git 操作。

## 精确恢复顺序

1. 完整重读`AGENTS.md`、`Base/Base.md`和全部规范 Task 文档；确认本任务仍只做到 M0，M1 延后。
2. 核验当前分支、本地/origin head、暂停记录提交、dirty 文件集合、writer lock、无活动 launcher/mutation、活动数据库 SHA-256、D 盘空间及三个保留 RUN ID 的不存在性；任何矛盾先停止并保留现场。
3. 只读完成异常 boundary 威胁模型文档复审；要求 P0=0、P1=0。
4. 依次运行 core、launcher、combined 三个 safe regression；解析`run-result.json`和 JUnit，同时核对所有安全门，不以 pytest passed 数量单独验收。
5. 若回归失败，保留 RUN，按唯一新编号修复；若通过，再重建最终 inventory、运行 targeted static/full safe 门、做独立暂存审计，之后才能显式提交和普通 push S3-F。
6. S3-F 正式验收前不得进入 S3-G；M0 完成前不得进入 M1，也不得连接 production writer 或处理真实资料。
