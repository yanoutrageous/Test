# M1 真实题库生产线与高保真排版阶段报告

## 结论

M1 于 2026-07-25 达到 `ACCEPTED`，可以进入 M2。正式基线是
`STATE-M1-YANYAN-REV-002` / `PAPER-YANYAN-202605-REV-002`；这不是 M5 最终用户验收。

REV-001 首次跑通全链路后发现 Office/Symbol 字体私用区字符会污染结构化答案，因此保留为历史回滚证据但不作为正式基线。REV-002 增加确定性字符归一化和视觉校正基线，结构化字段私用区字符计数为 0。

## 已实现

- 三份真实资料均先进入不可变 Copy，并校验来源台账、大小和 SHA-256；
- 真文本 PDF 分类、无文本失败路由、视觉行重组、1—19 题完整切分；
- 原貌整页图、19 个题目裁切、来源页/bbox/坐标空间/变换和内容哈希；
- 候选数据库默认 `pending`，经 Flask 本地复核 UI 逐题批准并记录 19 个事件；
- 题面、答案、解析、分值、`design_difficulty`、`observed_p=null` 和预计用时进入 QuestionIR；
- PaperIR、模板 revision、数据库状态和产物 manifest 均使用项目相对路径；
- 正式 PDF 使用 ADR 0001 的源页组合，重复渲染字节稳定；
- 应用重启后列表、详情和裁切资源均返回 200；
- 无文本输入进入复核且未改变活动数据库或正式派生树。

## 自动验证

- `RUN-20260725-M1-CORE-154`：核心切片 61/61 通过；
- `RUN-20260725-M1-GATE-157`：完整 M1 阶段门禁 466/466 通过；
- 活动数据库前后 SHA-256 均为
  `1505bf05bd8e385eada30642110596363c561c330da02a40a497072064ad1c94`；
- 候选库 19/19 题已批准，19/19 有答案，19/19 有解析；
- `PRAGMA integrity_check=ok`，外键违规为 0；
- 正式 PDF 4 页、总分 150，和源 Copy 字节哈希完全一致；
- 96 DPI 全页比较差异通道样本为 0，裁切/溢出/静默字体替换均为 0；
- 10 个源字体全部内嵌；
- 相同版本重复验证结果完全一致。

## 真实流程与视觉检查

真实流程证据为
`RUN-20260725-M1-REAL-PIPELINE-R2-153`，摘要 SHA-256 为
`06e9578c200c97b82b87f1bafbde0a01f5ba090d358946ad567ad8f6602bc01e`。

执行者在本地 UI 流程中修改第 1 题并批准全部 19 题；随后目视检查正式 revision 的第 1 页和第 4 页 144 DPI 图。标题、题号、公式、页脚和页面边界清晰，无截断、重叠或溢出。第 12、13 题答案另与参考答案首页原貌核对，分别固定为 `−1` 和 `x² − y² = 1`。

## 空间与可迁移性

REV-002 的版本数据库、派生资产和模板合计约 3.6 MiB；真实流水线完成后 E 盘可用空间约 85.5 GiB。运行路径只依赖 portable `PROJECT_ROOT` 和相对 manifest，不绑定 E 盘、用户名或父目录。

原卷、Copy、数据库、页面图、字体和正式 PDF 均由 `.gitignore` 排除；Git 只保存代码、测试、ADR、机器可读验收合同和小型摘要。

## 已知限制

- V1 正式 PDF 是高保真的源页组合，不是可编辑重排版；
- 本次真实切片是一份完整 A4 试卷，2020—2025 目标集扩展仍属于后续交付；
- 该真实试卷没有跨页题，跨页数据结构已实现并测试，但未在本卷真实触发；
- 结构化数学文本仍以检索和复核为目标，复杂公式的版式事实以原貌裁切为准。

## 回滚

REV-002 未切换或覆盖活动数据库。若后续 M2 失败，可继续使用原活动库，或按
`state-manifest.json` 只读打开 REV-002；REV-001 与 REV-002 均不可原位覆盖。
