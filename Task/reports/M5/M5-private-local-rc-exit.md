# M5 私有本地 RC1 交付报告

## 结论

`LOCAL-EXAM-BANK-1.0.0-RC1`已经发布到
`output/releases/LOCAL-EXAM-BANK-1.0.0-RC1`，达到
`ACCEPTED_SUPPORTED_PRIVATE_RC_SCOPE`：源资料权利人可直接在 Windows 11/NTFS
电脑上离线使用，也可把整个发布目录复制到另一台电脑后运行，不依赖仓库 `.venv`、
原盘符、用户名或开发者 PATH。

严格 M5 结论仍为 `BLOCKED_DISCLOSED`，不是失败掩盖：当前只有一份完成审核的
19 题、150 分代表卷；2020—2025 全目标集没有完成逐题生产和人工复核；来源派生内容
与嵌入字体没有第三方再分发许可；真实打印机和外部真实 PDF 导入也不在本次
Test-only 授权范围。当前包不得描述为全目标集或公开发行正式版。

## 直接使用

1. 进入 `output/releases/LOCAL-EXAM-BANK-1.0.0-RC1`。
2. 可先运行 `launcher\verify.cmd` 做 231 文件完整校验。
3. 双击 `launcher\start.cmd`，浏览器访问终端显示的
   `http://127.0.0.1:8765/`。
4. 首次启动只会在包内解压固定离线 Python 运行时；以后启动直接复用。
5. 数据、状态、备份、恢复 staging、导出和日志都留在产品根内。

## 已交付能力

- 统一入口与运行状态：回环监听、离线网络门、根目录/依赖/数据库/空间预检；
- 题库与复核：19 道审核题列表、详情、原貌、来源和字段编辑；
- 搜索与标签：条件、关键词、精确、本地相似检索，标签证据和审核；
- 蓝图组卷：求解、不可行诊断、锁题、换题、排序和状态持久化；
- 图形与模板：原图、SVG/TikZ 双轨、模板校准、缺字体/失败候选阻断；
- 五类输出：学生卷、教师卷、答案册、解析册和答题卡；
- 备份恢复：全量备份、staging 恢复、显式激活、救援状态和重启继续使用；
- 迁移与审计：固定离线运行时、SBOM、第三方许可、隐私扫描和 release manifest。

## 发布包

- 源码提交：
  `a4c153cb6ee4be68832dfc6f7a7fd06bf00a8b9f`；
- 231 个不可变文件，共 45,797,102 bytes；
- manifest SHA-256：
  `97652f875c46b7f3ab058ce49b3d1a9e262947904d1c6f186defbd2e77ef574e`；
- tree SHA-256：
  `92aac135916e0347a21d75325bbe5899e3eb24bccfc7f1b8327d4346d7db60ee`；
- Python 运行时归档 32,220,167 bytes，SHA-256：
  `c433356069ef7e5b26fcb7fd32568ee3eb5a431ca7741cec36c4e39474e7662c`；
- 隐私扫描：绝对路径、主机身份、PII、密钥模式均为 0；10 份 PDF 已扫描；
- 发布由 fresh-user 授权报告校验后使用 no-replace 目录移动完成，不覆盖已有成品。

## Fresh-user 客户验收

`RUN-20260726-M5-FRESH-R2-196`从无历史状态目录和无关 cwd 调用正式
PowerShell 启动器，完成 50 条公共 HTTP 用户旅程，失败 0：

- 冷启动与运行时安装；
- 首页、题库、详情、关键词/精确/相似检索和标签审核；
- 可行/不可行蓝图、锁题、换题和排序；
- 三类图形双轨和模板查看/校准/失败阻断；
- 五类 PDF 打开、渲染、字体嵌入和哈希；
- 全量备份、staging 恢复、显式激活、重启和状态继续；
- 恢复后五类 PDF 与首次生成逐份哈希一致。

Test 外写入 0、非回环网络成功 0、外部 HTML 引用 0；源保护树前后均为 812 个文件，
digest 同为
`95ed2e9e67f78407ec7ef47589455e2005da31b5daea704d17b9723c9dcb80ce`。
详细报告 SHA-256 为
`433b72f9e24d4996b48eb953b215da66aa59914179743e8d5384d92efa506e1e`，
发布授权 SHA-256 为
`061aea4bee018d2a2e2bb075252d739fe73ca0ddc6ee90cd9c5eb418e04b0358`。

## 五类 PDF

- 学生卷：4 页，384,634 bytes，
  `f7b374c5afe4adf86d285a6f7d20b6d74a0a2ec3f31850186273148aa17cc7aa`；
- 教师卷：15 页，1,087,404 bytes，
  `995aad0a326e15e96bb27032e5b83e9d1bf2182dd2cbcd217cb8b511491daf69`；
- 答案册：11 页，743,743 bytes，
  `cccf4751dd426092a49faa65d965085c1b54568fc880092989a74d65fdead9f5`；
- 解析册：63 页，2,308,349 bytes，
  `3a92ad698a63e5ecabed217bb27119017af4d5bdf7137adfe4b407f43f2362f5`；
- 答题卡：6 页，249,511 bytes，
  `999d9a8bcad62c6c00c93b1bf77d7e79d00645ee61ef7be4080a3f943e3f133a`。

五份文档未嵌入字体计数均为 0。自动验收只声明 PDF 预检、打开、渲染和打印就绪，
没有操作真实打印机。

## 最终门禁

`RUN-20260726-M5-FINAL-R1-197`通过 581/581，failure/error/skip 均为 0：

- 活动数据库前后 SHA-256 均为
  `1505bf05bd8e385eada30642110596363c561c330da02a40a497072064ad1c94`；
- 保护树前后均为 11,368 项，digest 均为
  `c4cec7960602d09ce4022c4ca2d2adf6e1cdfe8fc1754cc90a888f1ce3f2752c`；
- runtime watcher、句柄围栏、run tree、不可变证据、进程树和两份注册源终态全部通过；
- run result SHA-256：
  `a872124a8c3e5b595a327c2457ae1e1e38af0d58d419f4e0c64c3833f1bc28fd`；
- JUnit SHA-256：
  `8e052386b5b1d86e0da7266898cb528487352f2ccc421a1eb4a87e9645ae9102`。

同一源码上的编码修复核心门 `RUN-20260726-M5-CORE-R6-195`为 74/74。最终门已经覆盖
该回归，因此后续纯报告/状态提交不重复运行全量门。

## 浏览器视检

使用正式启动器启动已验收副本后，人工/浏览器视检了首页、19 题复核列表、蓝图页、
M3 工作台和五类文档页：首页与复核表格布局正常，入口可达，浏览器控制台
warning/error 为 0。

内置浏览器客户端自身阻止 `/workbench/search` 和 `/status.json` 直达导航；应用端
没有相应错误，同一路径已在 fresh-user 公共 HTTP 旅程中成功执行并纳入响应哈希。
该差异被记录但不判为产品缺陷。

## E 盘整理

验收后可恢复迁出 13 个被替代目录、2,785 个文件、331,726,797 bytes，包括：

- M3/M4 旧热门禁和 M5 被替代核心门；
- fresh-user 的完整产品/运行时/备份/恢复副本；
- 两个被替代的 M5 inventory 预览。

目标位于
`${USERPROFILE}/Codex-Recovery/Test-M5-20260726/post-acceptance-cleanup`。
13 个恢复目标均存在，E 盘原位置均已让出；逐文件相对路径/大小/SHA-256 聚合 digest
为
`79feb47763191d8bc1ab9b2db75916b0bb9bc8633f7ab143faecf37f6f177e28`。
E 盘继续保留最终 581 项门禁、小型 fresh-user 报告和 45.8 MB 最终包，整理后可用
114,989,887,488 bytes。

此前失败的构建和启动器编码现场也已分别迁到 C 盘恢复区，没有在 E 盘留下重复发布
副本。`.venv`约 103.6 MB，属于后续开发/验证环境且可重建，当前保留；最终用户包不
依赖它。

## 后续成本

普通使用不会重复构建、fresh-user 验收或 581 项门禁。首次启动只解压一次约 32.2 MB
压缩运行时；以后主要增长来自用户状态、导出和用户主动创建的备份。只有应用、依赖、
发布内容或安全边界变化时才需要重新构建和跑最终门禁。

## 未关闭的严格 M5 条件

- 生产并人工复核 2020—2025 新课标 I/II 与适应性测试全目标集；
- 解决 NEW9 无文字层但 V1 不做 OCR 的事实；
- 取得来源派生题目、解析、图形和嵌入字体的第三方再分发许可；
- 在另行授权后导入新的外部真实 PDF；
- 如要声称“实机打印验证”，另行授权打印池写入并记录驱动、纸张和物理测量。

在这些事实成立前，Draft PR 保持 Draft，不自动合并、不标记严格 M5 完成。
