# 依赖安装审计：GitHub CLI 2.96.0

## 被阻塞需求

- 阶段：PRE-M0 规划发布
- 需求：将规划和后续 M0—M5 阶段检查点普通 push 到`yanoutrageous/Test`并维护草稿 PR
- 阻塞事实：初始环境不存在`gh`命令；GitHub 发布规范要求安装并认证 GitHub CLI
- 用户授权：用户在 2026-07-11 本轮对话中明确授权安装本次必要组件，并要求以后仅在审计证明不安装会阻塞时安装

## 替代方案

- 仅使用`git push`：可能依赖未知凭据状态，不能完成规范化认证检查和草稿 PR，且不符合已选发布流程
- 手工 REST/API 或写入 token：风险更高，明确禁止
- 结论：安装官方 GitHub CLI 是当前最小必要方案

## 安装事实

- 包 ID：`GitHub.cli`
- 版本：`2.96.0`（文件版本日期 2026-07-02）
- 来源：Windows Package Manager `winget`官方源
- 下载地址（由 winget 输出）：`https://github.com/cli/cli/releases/download/v2.96.0/gh_2.96.0_windows_amd64.zip`
- winget：已报告安装包哈希验证成功
- 安装范围：用户范围，无 UAC/管理员权限
- 安装位置：`%LOCALAPPDATA%\Microsoft\WinGet\Packages\GitHub.cli_Microsoft.Winget.Source_8wekyb3d8bbwe\bin\gh.exe`
- 文件大小：41,504,056 字节
- `gh.exe` SHA-256：`CD79F16203F1FBE56937C4C96E2B6EADD10549418DCB241D91576AC77AF0AC8B`
- 许可：安装目录内`LICENSE`为 MIT License，Copyright 2019 GitHub Inc.
- 系统影响：用户 PATH/命令别名由 winget 更新；未安装驱动、服务或 Shell 扩展

## 验证

- `gh --version`（通过绝对路径）返回 2.96.0
- `winget list --id GitHub.cli --exact --source winget`确认 2.96.0
- `gh auth status`返回未登录任何 GitHub host
- 因未认证，本轮不得 push 或创建 PR；没有写入 token，也没有绕过认证

## 回滚

- 如需卸载：使用`winget uninstall --id GitHub.cli --exact --source winget`，但该操作应单独审计，当前不执行
- 项目规划文件不依赖`gh`运行；缺少它只阻塞远端发布

## 结论

- 风险等级：D2（用户范围必要工具安装）
- 安装结论：批准并完成
- 远端发布状态：BLOCKED_BY_AUTH
- 下一步：用户完成`gh auth login`，再核对`gh auth status`、仓库身份和 staged 范围后执行普通 push
