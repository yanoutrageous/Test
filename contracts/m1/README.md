# M1 验收合同

本目录只保存可提交 Git 的小型机器可读合同，不保存原卷、Copy、数据库、字体、页面图或正式 PDF。

`m1-acceptance-v1.json` 冻结 M1 正式基线的 revision、相对路径、哈希、门禁结果和已知限制。运行时大文件位于被 Git 忽略的 `data/` 与 `Copy/` 命名空间；迁移电脑时按 manifest 复制并复算哈希。

M1 `ACCEPTED` 只表示可以进入 M2，不代表 M5 最终用户验收或正式发布。
