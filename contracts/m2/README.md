# M2 验收合同

本目录只保存可提交 Git 的小型机器可读合同，不保存原卷、Copy、数据库、字体、
页面图或正式 PDF。

`m2-acceptance-v1.json` 冻结 M2 正式蓝图、候选池、PaperRevision、五件套 bundle、
真实失败注入、视觉/打印预检和阶段门禁。运行时大文件位于被 Git 忽略的
`data/exports/`；迁移电脑时按 bundle manifest 复制并复算哈希。

M2 `ACCEPTED` 只表示可以进入 M3，不代表 M5 最终用户验收或客户正式发布。
